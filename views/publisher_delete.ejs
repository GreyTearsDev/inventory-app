<h1><%= title%>: <%= publisher.name%></h1>

<% if (comic_list.length > 0) { %>
  <p><strong>Delete the following comics before attempting to delete this publisher:</strong></p>
  <div style='margin-left:20px;margin-top:20px'>
    <dl>
      <% comic_list.forEach(comic => { %>
        <dt><a href="<%= comic.url %>"><%= comic.title%></a></dt>
      <% }) %>
    </dl>
  </div>
<% } else { %>
  <p>Do you really want to delete this publisher?</p>
  <form method="POST">
    <div class="form-group"> 
      <input 
        id="publisherid" 
        class="form-control" 
        type="hidden" 
        name="publisherid" 
        value="<%= publisher.id%>" 
      />
    </div>
    <button class="btn btn-primary" type="submit">Delete</button>
  </form>
<% } %>
